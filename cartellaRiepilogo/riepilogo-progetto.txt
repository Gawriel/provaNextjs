================================================================================
  CINEMAVERSE (nome provvisorio) - RIEPILOGO PROGETTO
  Data: 31/07/2026
================================================================================

NOME PROGETTO (da confermare)
-----------------------------
Possibili brand: CinemaVerse | CinemaHub | MovieSphere | FrameFlow
Nel resto del documento si usa "CinemaVerse" come nome di lavoro.


OBIETTIVO FINALE
----------------
Costruire un'applicazione web completa stile Netflix + IMDb + sito cinema,
da usare come progetto portfolio.

L'utente potra:
  - navigare un catalogo film con filtri, ricerca e ordinamento
  - vedere la locandina / dettaglio di ogni film
  - registrarsi e fare login (JWT)
  - vedere UI diversa da loggato (avatar con iniziali in alto a destra)
  - gestire profilo, preferiti, logout
  - aprire una pagina di streaming finto (/movies/[id]/watch)
  - prenotare posti al cinema (mappa posti colorata)
  - se ADMIN: CRUD completo sui film (sostituisce l'Employee Manager)

Dal punto di vista tecnico si dimostrano:
  Next.js, React, TypeScript, API REST (Route Handlers),
  Prisma, MongoDB, Login/JWT, CRUD, filtri, ricerca, routing,
  componenti riusabili, stato/context, deploy.


ARCHITETTURA SCELTA
-------------------
Next.js + API Routes (SENZA Express separato).

  Browser (React)
        |
        |  fetch("/api/...")
        v
  Next.js (stesso progetto)
    - app/           -> pagine UI (frontend)
    - app/api/       -> backend REST (Route Handlers)
        |
        v
     Prisma
        |
        v
     MongoDB

Un solo npm run dev, una sola porta (3000), un solo deploy (es. Vercel)
+ MongoDB Atlas per il database.


================================================================================
  COSA C'E OGGI NEL PROGETTO
================================================================================

Cartella root:
  progettoReactNextJs/
    cartellaRiepilogo/     <- questa cartella (documentazione)
    reactnextjs/           <- app Next.js creata con create-next-app

Stack gia installato in reactnextjs:
  - next 16.x
  - react 19.x / react-dom 19.x
  - typescript
  - tailwindcss 4
  - eslint + eslint-config-next

File/cartelle attuali in reactnextjs (base create-next-app):
  app/
    layout.tsx       -> layout globale (font, metadata, html/body)
    page.tsx         -> homepage di default Next (da sostituire)
    globals.css      -> stili globali + Tailwind
    favicon.ico
  public/            -> asset statici (svg di esempio Next)
  package.json       -> dipendenze e script (dev, build, start, lint)
  package-lock.json
  tsconfig.json      -> TypeScript + alias @/*
  next.config.ts
  next-env.d.ts
  postcss.config.mjs
  eslint.config.mjs
  README.md
  node_modules/      -> pacchetti installati

COSA NON C'E ANCORA:
  - Prisma / MongoDB
  - cartella app/api/
  - components/ custom
  - auth / JWT
  - pagine movies, login, profile, admin
  - dati film (neanche statici)
  - .env di progetto (DATABASE_URL, JWT_SECRET, DEBUG_*)
  - logica CinemaVerse


================================================================================
  STRUTTURA TARGET (da raggiungere nel tempo)
================================================================================

reactnextjs/
  app/
    layout.tsx
    page.tsx                          # Home brand
    globals.css
    movies/
      page.tsx                        # Catalogo
      [id]/
        page.tsx                      # Dettaglio / locandina
        watch/page.tsx                # Streaming finto
        book/page.tsx                 # Prenotazione posti
    login/page.tsx
    register/page.tsx
    profile/page.tsx
    admin/movies/
      page.tsx                        # CRUD admin
      [id]/edit/page.tsx
    api/                              # BACKEND
      auth/login/route.ts
      auth/register/route.ts
      auth/me/route.ts
      movies/route.ts                 # GET lista, POST crea
      movies/[id]/route.ts           # GET/PUT/DELETE
      favorites/route.ts
      reviews/route.ts
      reservations/route.ts
  components/
    layout/   (Header, Footer, UserAvatar)
    movies/   (MovieCard, MovieFilters, MovieGrid, MoviePlayer)
    auth/     (LoginForm, RegisterForm)
    booking/  (SeatMap)
    ui/       (button, input, dialog riusabili)
  lib/
    prisma.ts
    auth.ts
    debug.ts                          # DEBUG_ENABLE + DEBUG_LEVEL 1-4
  context/
    AuthContext.tsx
  types/
    movie.ts, user.ts, reservation.ts
  data/
    movies.ts                         # SOLO Fase 1 (array statico)
  prisma/
    schema.prisma
  .env / .env.example


MODELLO DATI (MongoDB via Prisma)
---------------------------------
  User         -> nome, email, passwordHash, role (USER | ADMIN)
  Movie        -> titolo, descrizione, anno, durata, genere,
                  poster, trailer, streaming
  Favorite     -> userId + movieId
  Review       -> userId + movieId + voto + commento
  Reservation  -> userId + movieId + cinema + data + posti


================================================================================
  FASI DI LAVORO (ORDINE CONSIGLIATO)
================================================================================

NON partire dal login. Ordine:

  FASE 1 - Catalogo statico
    Imparare Next, routing, componenti, layout, filtri UI.
    Dati da array/JSON locale (data/movies.ts). Niente DB.

  FASE 2 - MongoDB + Prisma
    Collegare il database. I film arrivano da Mongo (ancora senza API
    complete se si vuole leggere via server component, oppure subito API).

  FASE 3 - API Routes film
    CRUD REST in app/api/movies. Frontend fa fetch("/api/movies").

  FASE 4 - Admin CRUD (senza auth)
    Pagine admin per aggiungere/modificare/eliminare film.
    Capire il Movie Manager puro.

  FASE 5 - Login / Registrazione / JWT
    Proteggere operazioni admin e personalizzare l'utente.

  FASE 6 - Profilo, avatar iniziali, preferiti
    Context utente, header fisso con iniziali, logout.

  FASE 7 - Streaming finto
    /movies/[id]/watch con player mock (YouTube trailer o video locale).

  FASE 8 - Prenotazione cinema
    Scelta cinema -> orario -> posti (mappa) -> prenota.

  FASE 9 - Deploy
    Vercel (app) + MongoDB Atlas (DB). Portfolio online.


================================================================================
  CHECKLIST FASE 1 - FILE / COSE DA CREARE
  (prossimo lavoro concreto - catalogo statico)
================================================================================

[ ] 1. Decidere il nome brand definitivo (es. CinemaVerse)

[ ] 2. Creare lib/debug.ts
       - legge DEBUG_ENABLE (env o variabile locale)
       - legge DEBUG_LEVEL (1-4, default 4)
       - logga solo se enable=true

[ ] 3. Creare types/movie.ts
       - tipo Movie (id, titolo, descrizione, anno, durata, genere,
         poster, trailer, streaming, regista, attori)

[ ] 4. Creare data/movies.ts
       - array di 8-12 film fake (dati inventati o pubblici)

[ ] 5. Creare components/layout/Header.tsx
       - logo/brand, link Catalogo, Login/Registrati (placeholder)

[ ] 6. Creare components/layout/Footer.tsx

[ ] 7. Aggiornare app/layout.tsx
       - inserire Header + Footer
       - metadata col nome brand
       - lingua it

[ ] 8. Sostituire app/page.tsx
       - home con brand + CTA verso /movies
       - niente logica auth ancora

[ ] 9. Creare components/movies/MovieCard.tsx
       - props configurabili (movie, onClick/href)

[ ] 10. Creare components/movies/MovieGrid.tsx

[ ] 11. Creare components/movies/MovieFilters.tsx
        - genere, anno, durata, ricerca testo, ordinamento (UI only)

[ ] 12. Creare app/movies/page.tsx
         - catalogo che legge data/movies.ts
         - applica filtri lato client

[ ] 13. Creare app/movies/[id]/page.tsx
         - dettaglio / locandina
         - link placeholder "Guarda" e "Prenota" (ancora non funzionanti)

[ ] 14. Creare .env.example
         - DEBUG_ENABLE=true
         - DEBUG_LEVEL=4
         - (piu avanti DATABASE_URL, JWT_SECRET)

[ ] 15. Aggiornare .gitignore se serve (gia presente di solito)

[ ] 16. Verificare: npm run dev, homepage + catalogo + dettaglio ok
         hot reload senza errori


CHECKLIST FASI SUCCESSIVE (sintesi)
-----------------------------------
Fase 2: [ ] account Mongo Atlas  [ ] npm i prisma @prisma/client
        [ ] prisma/schema.prisma  [ ] lib/prisma.ts  [ ] seed film

Fase 3: [ ] app/api/movies/route.ts  [ ] app/api/movies/[id]/route.ts
        [ ] frontend passa da data/ a fetch("/api/...")

Fase 4: [ ] app/admin/movies  [ ] form create/edit  [ ] dialog conferma delete

Fase 5: [ ] register/login API  [ ] JWT  [ ] middleware/protezione admin

Fase 6: [ ] AuthContext  [ ] UserAvatar  [ ] profile  [ ] favorites API

Fase 7: [ ] watch/page.tsx  [ ] MoviePlayer

Fase 8: [ ] book/page.tsx  [ ] SeatMap  [ ] reservations API

Fase 9: [ ] deploy Vercel  [ ] README portfolio  [ ] demo online


================================================================================
  REGOLE DI PROGETTO (CONVENZIONI)
================================================================================

- Componenti riusabili e configurabili via props; spezzare se > 300-400 righe.
- Form e finestre di dialogo = componenti a parte.
- React NON parla col database: solo le API Routes usano Prisma.
- Debug: log solo se DEBUG_ENABLE (env O locale) e true; livello 1-4 (default 4).
- Dopo ogni modifica significativa: proporre un commit sulla repo GitHub.


================================================================================
  PROSSIMI STEP IMMEDIATI
================================================================================

1. Confermare il nome brand.
2. Eseguire la CHECKLIST FASE 1 (file elencati sopra).
3. Avere un'app navigabile: Home -> Catalogo -> Dettaglio film (dati statici).
4. Solo dopo: introdurre Prisma + MongoDB (Fase 2).


================================================================================
  FINE RIEPILOGO
================================================================================
